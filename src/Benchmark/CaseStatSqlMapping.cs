using Coding4fun.DataTableGenerator.Common;

namespace Coding4fun.DataTableGenerator.Benchmark
{
    public partial class CaseStatSqlMapping
    {
        static CaseStatSqlMapping()
        {
            //new DataTableTableBuilder<CaseStat>();
            // .AddColumn("iso_code", "VARCHAR(MAX)", c => c.iso_code)
            // .AddColumn("continent", "VARCHAR(MAX)", c => c.continent)
            // .AddColumn("location", "VARCHAR(MAX)", c => c.location)
            // .AddColumn("date", "VARCHAR(MAX)", c => c.date)
            // .AddColumn("total_cases", "VARCHAR(MAX)", c => c.total_cases)
            // .AddColumn("new_cases", "VARCHAR(MAX)", c => c.new_cases)
            // .AddColumn("new_cases_smoothed", "VARCHAR(MAX)", c => c.new_cases_smoothed)
            // .AddColumn("total_deaths", "VARCHAR(MAX)", c => c.total_deaths)
            // .AddColumn("new_deaths", "VARCHAR(MAX)", c => c.new_deaths)
            // .AddColumn("new_deaths_smoothed", "VARCHAR(MAX)", c => c.new_deaths_smoothed)
            // .AddColumn("total_cases_per_million", "VARCHAR(MAX)", c => c.total_cases_per_million)
            // .AddColumn("new_cases_per_million", "VARCHAR(MAX)", c => c.new_cases_per_million)
            // .AddColumn("new_cases_smoothed_per_million", "VARCHAR(MAX)", c => c.new_cases_smoothed_per_million)
            // .AddColumn("total_deaths_per_million", "VARCHAR(MAX)", c => c.total_deaths_per_million)
            // .AddColumn("new_deaths_per_million", "VARCHAR(MAX)", c => c.new_deaths_per_million)
            // .AddColumn("new_deaths_smoothed_per_million", "VARCHAR(MAX)", c => c.new_deaths_smoothed_per_million)
            // .AddColumn("reproduction_rate", "VARCHAR(MAX)", c => c.reproduction_rate)
            // .AddColumn("icu_patients", "VARCHAR(MAX)", c => c.icu_patients)
            // .AddColumn("icu_patients_per_million", "VARCHAR(MAX)", c => c.icu_patients_per_million)
            // .AddColumn("hosp_patients", "VARCHAR(MAX)", c => c.hosp_patients)
            // .AddColumn("hosp_patients_per_million", "VARCHAR(MAX)", c => c.hosp_patients_per_million)
            // .AddColumn("weekly_icu_admissions", "VARCHAR(MAX)", c => c.weekly_icu_admissions)
            // .AddColumn("weekly_icu_admissions_per_million", "VARCHAR(MAX)",
            //     c => c.weekly_icu_admissions_per_million)
            // .AddColumn("weekly_hosp_admissions", "VARCHAR(MAX)", c => c.weekly_hosp_admissions)
            // .AddColumn("weekly_hosp_admissions_per_million", "VARCHAR(MAX)",
            //     c => c.weekly_hosp_admissions_per_million)
            // .AddColumn("new_tests", "VARCHAR(MAX)", c => c.new_tests)
            // .AddColumn("total_tests", "VARCHAR(MAX)", c => c.total_tests)
            // .AddColumn("total_tests_per_thousand", "VARCHAR(MAX)", c => c.total_tests_per_thousand)
            // .AddColumn("new_tests_per_thousand", "VARCHAR(MAX)", c => c.new_tests_per_thousand)
            // .AddColumn("new_tests_smoothed", "VARCHAR(MAX)", c => c.new_tests_smoothed)
            // .AddColumn("new_tests_smoothed_per_thousand", "VARCHAR(MAX)", c => c.new_tests_smoothed_per_thousand)
            // .AddColumn("positive_rate", "VARCHAR(MAX)", c => c.positive_rate)
            // .AddColumn("tests_per_case", "VARCHAR(MAX)", c => c.tests_per_case)
            // .AddColumn("tests_units", "VARCHAR(MAX)", c => c.tests_units)
            // .AddColumn("total_vaccinations", "VARCHAR(MAX)", c => c.total_vaccinations)
            // .AddColumn("people_vaccinated", "VARCHAR(MAX)", c => c.people_vaccinated)
            // .AddColumn("people_fully_vaccinated", "VARCHAR(MAX)", c => c.people_fully_vaccinated)
            // .AddColumn("total_boosters", "VARCHAR(MAX)", c => c.total_boosters)
            // .AddColumn("new_vaccinations", "VARCHAR(MAX)", c => c.new_vaccinations)
            // .AddColumn("new_vaccinations_smoothed", "VARCHAR(MAX)", c => c.new_vaccinations_smoothed)
            // .AddColumn("total_vaccinations_per_hundred", "VARCHAR(MAX)", c => c.total_vaccinations_per_hundred)
            // .AddColumn("people_vaccinated_per_hundred", "VARCHAR(MAX)", c => c.people_vaccinated_per_hundred)
            // .AddColumn("people_fully_vaccinated_per_hundred", "VARCHAR(MAX)",
            //     c => c.people_fully_vaccinated_per_hundred)
            // .AddColumn("total_boosters_per_hundred", "VARCHAR(MAX)", c => c.total_boosters_per_hundred)
            // .AddColumn("new_vaccinations_smoothed_per_million", "VARCHAR(MAX)",
            //     c => c.new_vaccinations_smoothed_per_million)
            // .AddColumn("stringency_index", "VARCHAR(MAX)", c => c.stringency_index)
            // .AddColumn("population", "VARCHAR(MAX)", c => c.population)
            // .AddColumn("population_density", "VARCHAR(MAX)", c => c.population_density)
            // .AddColumn("median_age", "VARCHAR(MAX)", c => c.median_age)
            // .AddColumn("aged_65_older", "VARCHAR(MAX)", c => c.aged_65_older)
            // .AddColumn("aged_70_older", "VARCHAR(MAX)", c => c.aged_70_older)
            // .AddColumn("gdp_per_capita", "VARCHAR(MAX)", c => c.gdp_per_capita)
            // .AddColumn("extreme_poverty", "VARCHAR(MAX)", c => c.extreme_poverty)
            // .AddColumn("cardiovasc_death_rate", "VARCHAR(MAX)", c => c.cardiovasc_death_rate)
            // .AddColumn("diabetes_prevalence", "VARCHAR(MAX)", c => c.diabetes_prevalence)
            // .AddColumn("female_smokers", "VARCHAR(MAX)", c => c.female_smokers)
            // .AddColumn("male_smokers", "VARCHAR(MAX)", c => c.male_smokers)
            // .AddColumn("handwashing_facilities", "VARCHAR(MAX)", c => c.handwashing_facilities)
            // .AddColumn("hospital_beds_per_thousand", "VARCHAR(MAX)", c => c.hospital_beds_per_thousand)
            // .AddColumn("life_expectancy", "VARCHAR(MAX)", c => c.life_expectancy)
            // .AddColumn("human_development_index", "VARCHAR(MAX)", c => c.human_development_index)
            // .AddColumn("excess_mortality", "VARCHAR(MAX)", c => c.excess_mortality);
        }
    }
}